export type Workspace = {
  id: string;
  user_id: string;
  name: string;
  is_favorite: boolean;
  created_at: string;
  updated_at: string;
};

// We will add Card, Group, Edge types later in Phase 1.3
